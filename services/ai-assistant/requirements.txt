# AI assistant runtime deps. Lock-consistent with the root pyproject.toml /
# poetry.lock. openai / langchain / langgraph removed: not in the lock file and
# not currently imported by this service (add them to the root pyproject when LLM
# integration is wired). python-json-logger removed (structlog is the logger).
qdrant-client
sentence-transformers
pydantic
pydantic-settings
numpy
confluent-kafka
structlog
asyncpg
